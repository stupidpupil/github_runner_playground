name: Playground

on:
  push:
  workflow_dispatch:

permissions: {}

jobs:
  playground:
    runs-on: "ubuntu-24.04-arm"
    steps:

      - name: Playground
        run: |
          lsblk
          free
          swapon
          lsmod